[package]
name = "battle_simulator"
version = "0.1.0"
edition = "2021"

[lib]
name = "battle_simulator"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22.2", features = ["extension-module"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
